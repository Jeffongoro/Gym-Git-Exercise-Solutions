# GIT Exercise

## Bundle 1

### Exercise 1

```bash
git init
git status
git branch -M main
git status
git add .
git commit -m "my first commit"
git remote add origin https://github.com/Jeffongoro/Gym-Git-Exercise-Solutions.git
git push -u origin main
git checkout -b dev
git status
git push origin dev
git checkout -b test
git push origin test
git checkout dev
git branch -D test
git push origin --delete test
```
